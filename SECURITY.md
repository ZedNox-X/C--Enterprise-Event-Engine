# Security

Reference implementation only. Production use requires TLS/mTLS, authenticated APIs, secret management, network policies, least-privilege database access, image scanning, and strict validation of all external events. Never commit credentials or private keys.
